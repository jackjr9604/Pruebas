// ESPRESIONES ()
//Comparaciones bolleanas
  // viene la logica booleana
    1 < 2 //true
// (el = es de asignacion y == es decir q algo es igual a algo)
  // 1 = 2 da Error!! por q 1 no puede igualarse a dos simplemente
  1 == 2 //false


// STATEMENT (no tiene un resultado)
  // Flujo de control
  if (condicion) {
    // codigo
  }

  if (1 < 2) {
    console.log('uno es menor que dos');// esto es una expresion dentro de un statement y se va a imprimir si la condicion es true
  }

  if (1 > 2) {
    console.log('uno es menor que dos');
  }// no se va a imprimira nada si la condicion es falsa

  //( && = y )
